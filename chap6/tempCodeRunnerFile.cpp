  // sort(avector.begin(), avector.end());
  // sort(bvector.begin(), bvector.end());
  // sort(cvector.begin(), cvector.end());

  // for (int i = 0; i < N; ++i) {
  //   int taraget_a = avector[i];
  //   auto iter = lower_bound(bvector.begin(), bvector.end(), taraget_a);
  //   int val = *iter;
  //   for (int t = val; t < N; ++t) {
  //     int target_b = bvector[t];
  //     int counter = cvector.end() - lower_bound(cvector.begin(), cvector.end(), target_b);
  //     result += counter;
  //   }
  // }
  // cout << result << endl;